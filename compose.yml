services:
  app:
    image: jenmchiang/docker-demo:v2.1
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgres://user:password@db:5432/messages
    depends_on:
      - db

  db:
    image: postgres:15-alpine
    environment:
      # the following would typically be in .env files
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: messages
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  pgdata:
