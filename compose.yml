services:
  app:
    image: jenmchiang/docker-demo:v2.1
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgres://user:password@db:5432/messages
    depends_on:
      - db
    volumes:
      # stores local app into the app dir in container
      - ./app:/app
    # run command inside container to capture dependencies
    command: sh -c "npm install && npm start"
    # networks:
    #   - appnet

  db:
    image: postgres:15-alpine
    environment:
      # the following would typically be in .env files
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: messages
    volumes:
      - pgdata:/var/lib/postgresql/data
    # networks:
    #   - appnet

volumes:
  pgdata:

# networks:
#   appnet:
#     driver: bridge
